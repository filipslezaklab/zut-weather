{{ "city,country,date,celsius,fahrenheit" }}</br>
{% for measurement in measurements %}
{{ city }},{{ country }},{{ measurement.date | date('Y-m-d') }},{{ measurement.temperature }},{{ measurement.getFahrenheit() }}</br>
{% endfor %}